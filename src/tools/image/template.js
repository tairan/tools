import { select, button, textInput, output, cancel, notice, note } from '../../../build/fields.js';
export default function imageWorkspace() {
  return `<section class="file-zone" id="image-drop"><label for="file-input">选择图片，或将图片拖到这里</label><input id="file-input" type="file" accept="image/jpeg,image/png,image/webp" multiple><p>JPG、PNG、WebP；每批最多 20 张，每张不超过 30 MiB。</p></section><div class="tool-options">${select('image-format', '输出格式', [['image/webp', 'WebP'], ['image/jpeg', 'JPG'], ['image/png', 'PNG']])}<label class="field" for="image-quality"><span>质量 <output id="quality-value">80%</output></span><input id="image-quality" type="range" min="10" max="100" value="80"></label><label class="field" for="image-width"><span>最长边（px）</span><input id="image-width" type="number" min="1" max="16384" step="1" placeholder="留空保留尺寸"></label></div><div class="actions">${button('process', '处理图片', 'class="primary" disabled')}${cancel}${button('clear', '清空')}${button('download-all', '下载全部 ZIP', 'disabled')}</div><progress id="progress" value="0" max="100" hidden aria-label="图片处理进度"></progress>${notice}<ul id="image-results" class="image-results" aria-label="图片处理结果"></ul>${note('默认保持原尺寸，输出 WebP、质量 80%。缩放保持比例且不放大；PNG 为无损编码。JPEG 的透明背景填充白色。输出不保留原图元数据；动画图片仅导出静态画面。实际大小可能增加，请比较后下载。')}`;
}
