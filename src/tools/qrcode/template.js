import { select, button, textInput, output, cancel, notice, note } from '../../../build/fields.js';
export default function qrWorkspace() {
  return `<div class="generator-layout"><section><label class="field" for="input"><span>文字或链接</span><textarea id="input" rows="6" placeholder="例如 https://tairan.org/" spellcheck="false" autocomplete="off"></textarea></label><div class="settings-fields">${select('qr-size', '导出尺寸', [['256', '256 px'], ['512', '512 px'], ['1024', '1024 px']])}${select('qr-level', '纠错等级', [['M', 'M · 约 15%'], ['L', 'L · 约 7%'], ['Q', 'Q · 约 25%'], ['H', 'H · 约 30%']])}</div><div class="actions">${button('generate', '生成二维码', 'class="primary"')}${button('clear', '清空')}</div></section><section class="qr-result" aria-label="二维码结果"><p id="qr-empty">输入内容后，生成你的二维码。</p><canvas id="qr-canvas" hidden aria-label="生成的二维码"></canvas><div class="actions">${button('download-png', '下载 PNG', 'disabled')}${button('download-svg', '下载 SVG', 'disabled')}</div></section></div>${notice}${note('黑白配色与四周留白保证可扫描性。内容过长时请缩短文字，或降低纠错等级；链接原文直接编码，不创建短网址。')}`;
}
