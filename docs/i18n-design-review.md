# 三语言版本发布前设计检查

2026-09-15，针对本地生产构建；结论：通过，无 P0/P1 阻断项。

## 检查结果

| 维度 | 分数 | 证据与范围 |
| --- | --- | --- |
| 无障碍 | 3/4 | 键盘跳转正文、表单可见标签、状态提示与主题文字对比度测试通过；未进行真实屏幕阅读器认证。 |
| 性能 | 4/4 | 目录不加载 PDF、ZIP、哈希等专用计算依赖；工具按需加载、本地处理，未新增远程翻译请求。 |
| 响应式 | 3/4 | 全部页面三种语言在 320px、1440px 无横向溢出；原有四档宽度和浅深主题测试通过。部分紧凑控件低于 44px。 |
| 主题 | 4/4 | 浅深主题、系统主题与偏好保持正常，语义颜色对比度测试通过。 |
| 实现一致性 | 4/4 | 保留方庭标志、景泰蓝和平面目录；语言切换保留输入、文件、结果及进行中的任务。 |
| 合计 | 18/20 | 通过；存在非阻断改进项。 |

## 视觉与交互证据

检查 Chromium 生成的英文桌面目录与日文 320px 随机字符串页截图：标题、分类、说明、设置、结果和页尾层级清楚，译文正常换行。三语全页面布局由 Chromium、Firefox、WebKit 自动验证；浅深主题由原有功能回归覆盖。测试截图位于被忽略的 test-results/。

语言自动识别、存储不可用、跨页面偏好、参数化错误、处理中的语言切换、图片与 PDF 结果保留均有浏览器测试。真实触屏设备和屏幕阅读器未测试；桌面浏览器的窄视口检查不代表真实触控测试。

## 非阻断项

- P2：页头选择器 40px、编辑器辅助按钮 36px，低于 44px 触控舒适目标；仍有独立可见标签、键盘焦点和操作间隔。这不是据此认定的 WCAG AA 不合格。后续可统一扩大紧凑控件。
- P3：机械检测器返回 9 条字体相关提示。23/26/29px 等响应式字号及日文字体已在 DESIGN.md 正文说明，但不全在前置 token 中；12/18px 等局部字号也未进入统一字号表。实测没有排版缺陷，未通过屏蔽规则消除提示；后续可补齐机器可读的排版角色。

## 发布验证

- npm test：16 项通过。
- npm run build：通过。
- npm run test:browser：63 项通过（21 场景 × 3 浏览器）。
- git diff --check：通过。

本记录表示发布前检查通过，实际部署结果需另行确认。
